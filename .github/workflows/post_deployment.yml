---
name: Post Deployment

on:
  workflow_run:
    workflows: ["db-migrate"]
    types:
      - completed
  workflow_call:

jobs:
  verify-deployment:
    runs-on: ubuntu-latest
    name: Verify deployed API
    steps:
        - name: Checkout code
          uses: actions/checkout@v3
        - run: echo "post deplotment compelte"
